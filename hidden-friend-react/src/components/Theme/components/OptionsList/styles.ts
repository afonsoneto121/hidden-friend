import { styled } from "@material-ui/system";

export const Container = styled('div')({
  width: '100%',
  paddingTop: 10,
  'ul': {
    margin: 0,
    padding: 0,
    'li': {
      margin: 0,
      padding: 0,
    }
  }
})